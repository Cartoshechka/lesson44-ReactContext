# 👥 React Context App - Управление Пользователями (TypeScript)

Демонстрационный проект использования React Context API с TypeScript для управления глобальным состоянием в многоуровневой архитектуре компонентов.

## 🚀 Особенности

- ✅ **TypeScript**: Полная типизация для надежности кода.
- ✅ **Множественные контексты**: Раздельное управление пользователями и темой оформления.
- ✅ **Кастомные крючки (Hooks)**: `useUser` и `useTheme` со встроенной проверкой наличия провайдера.
- ✅ **Оптимизация**: Использование `React.memo`, `useMemo` и `useCallback` для предотвращения лишних ререндеров.
- ✅ **3 уровня вложенности**: Демонстрация передачи данных через `App → Header/Main → UserList → UserCard`.
- ✅ **Темизация**: Переключение между светлой и темной темой с сохранением в `localStorage`.

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск проекта в режиме разработки
npm run dev
```

## 🏗️ Структура проекта

```
src/
├── types/
│   └── index.ts              # Типы TypeScript
├── context/
│   ├── UserContext.tsx       # Контекст пользователей + useUser
│   └── ThemeContext.tsx      # Контекст темы + useTheme (с сохранением в localStorage)
├── components/
│   ├── Header.tsx            # Компонент заголовка с переключателем темы
│   ├── UserList.tsx          # Список пользователей
│   ├── UserCard.tsx          # Карточка отдельного пользователя
│   └── UserProfile.tsx       # Детальная информация о выбранном пользователе
└── App.tsx                   # Главный компонент, объединяющий провайдеры
```

## 🎯 Ключевые концепции

### Кастомные хуки с проверкой

```typescript
const { theme, toggleTheme } = useTheme() // Автоматическая проверка на наличие ThemeProvider
```

### Типизированные контексты

```typescript
interface UserContextType {
  users: User[]
  selectedUser: number | null
  selectUser: (userId: number) => void
  addUser: (user: Omit<User, 'id'>) => void
}
```

### Сохранение состояния

Тема оформления автоматически сохраняется в `localStorage`, поэтому она не сбрасывается при обновлении страницы.
